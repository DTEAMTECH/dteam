import React, {useEffect} from 'react';

const Features = () => {

    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return (
        <section className="container">

        </section>
    );
};

export default Features;