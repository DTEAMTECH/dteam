import React, {FC, useEffect} from 'react';
const AboutUs:FC = () => {

    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return (
        <section className="container">

        </section>
    );
};

export default AboutUs;